{
  "dependencies": {
    "token-transformer": "^0.0.24"
  },
  "devDependencies": {
    "style-dictionary": "^3.7.1"
  },
  "scripts": {
    "generate": "token-transformer tokens.json output.json"
  }
}
